/**
 * Re-export for backward compatibility.
 * Implementation lives under `venues/` (types, services, hooks, components).
 */
export { default } from "./venues/VenueManagementPage"
export type { Venue, VenueTab, VenueListingStatus } from "./venues/types/venue.types"
