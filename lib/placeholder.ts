/**
 * Fallback when an image URL is missing.
 * Use this instead of `/placeholder.svg` (avoided slow dev requests to that route).
 */
export const DEFAULT_IMAGE_PLACEHOLDER = "/city/c4.jpg"

/** VIP hero, featured, trending, and other home event cards when no banner is set. */
export const EVENT_IMAGE_FALLBACK = DEFAULT_IMAGE_PLACEHOLDER
